# 安全说明

## 当前安全状态

本项目尚未通过独立安全测试或正式渗透测试。2026-08-15 已完成第一批 P0 代码修复，但订单信任边界、支付/退款回调、危险脚本和历史凭据轮换等 P0 仍未完成，因此当前版本不应被视为“可直接安全对公网发布”的通用产品。真实支付、会员隐私和生产数据库接入前，至少先完成 [已知问题](./docs/known-issues.md) 中剩余的 P0 清单。

审计结论来自当前仓库代码，不代表部署环境、云防火墙、反向代理或线上数据库已被验证。

## 高风险项与阶段状态

1. 仓库本地遗留文档、历史脚本和已跟踪前端代码中出现过真实云密钥、第三方 API key 或固定默认凭据。车型页面中的固定第三方 key 已从客户端移除并改由后端环境变量代理，但所有出现过的值仍应按“已经泄露”处理：立即在服务商侧轮换并检查调用日志。删除当前代码不能代替轮换，也不要把旧秘密仅仅移动到另一个 Markdown 文件。
2. 第一阶段已为本轮列出的内容、商品、会员配置和队列写接口补齐 `AdminGuard` 与真实权限键；队列状态写接口也已收紧为管理员权限。公开队列响应已改为服务端白名单和车牌遮罩。其他 Controller 仍需后续持续审计。
3. 公开订单创建接口信任客户端提交的会员、价格、折扣和数量，订单详情接口缺少完整的所有权校验。
4. 第一阶段已修复公开改手机号路径：只接受有效会员 Bearer token，从 token 推导 memberId，同时校验当前手机号和新手机号验证码，并限制尝试次数与重放。旧客户端的单验证码请求不再兼容。
5. 微信支付/退款回调的签名、商户号、appid 和金额核对不完整。
6. `apps/api/scripts/deploy-production.sh` 会重置数据库；多个 seed、初始化、删除和 backfill 脚本包含固定配置、绕过业务补偿或与当前 Prisma 7 不兼容。

具体路径、影响和建议顺序见 [docs/known-issues.md](./docs/known-issues.md)。

## 密钥与配置规则

- 真实 `.env`、证书、私钥、token、Cookie、数据库连接串和云密钥不得提交到 Git。
- 示例只允许写入 `.env.example`，且必须使用不可用的占位值。
- 所有 `VITE_*` 都会进入客户端产物，必须视为公开信息。服务端密钥不得使用 `VITE_*`。
- 微信商户私钥和证书只保存在部署机的受限目录，通过环境变量传入路径；不要放进仓库。
- 日志和错误信息不得输出 Authorization、验证码、密码、完整手机号、openId 或支付解密内容。
- 发现秘密进入 Git 后，仅删除最新文件不够；应先轮换，再评估是否需要清理 Git 历史。

## 生产操作规则

- 执行任何迁移、seed、backfill、物理删除或文件清理前，先确认目标数据库主机、库名和环境，完成可恢复备份，并在副本演练。
- 生产环境只应用经过审阅的 Prisma migration；不得运行 `migrate reset`、`db push` 或带 `--accept-data-loss` 的命令。
- 不得把 `apps/api/scripts/deploy-production.sh` 当作部署入口。
- 数据库与 `uploads` 目录必须作为一个一致的备份/恢复单元。
- Swagger `/docs`、本地上传 `/uploads` 和宽松 CORS 当前默认公开；生产反向代理必须根据业务需要限制访问，代码层也应补足权限。

## P0 修复完成的最低验收

- 所有写接口都有明确的身份、角色和权限策略，并对公开读接口做字段最小化与所有权校验。
- 下单价格、库存、优惠、积分和会员归属由服务端根据数据库重新计算；DTO 拒绝负数、异常数量和未知字段。
- 改手机号同时验证当前会员身份、旧手机号控制权和新手机号验证码。
- 微信回调按官方流程验证签名，并核对商户号、appid、订单号、金额和幂等状态。
- 所有历史真实密钥和固定默认凭据已轮换；seed 改为显式输入一次性管理员信息，且不会在重复运行时重置密码。
- 危险脚本已删除或加入目标校验、dry-run、交互确认、备份检查与可审计输出。
- 至少建立鉴权、下单、支付回调、退款、库存/优惠券补偿的自动化回归测试。

## 报告安全问题

请使用私下渠道联系仓库维护者，不要在公开 Issue 中粘贴可复现的真实密钥、会员数据或支付报文。报告应包含受影响路径、最小复现条件、影响范围和建议修复方向；所有示例数据必须脱敏。
